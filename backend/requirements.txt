fastapi
uvicorn[standard]
sqlalchemy>=2.0
# PostgreSQL driver:
# - psycopg2-binary often lags for newest Python versions
# - psycopg (v3) supports newer Pythons sooner
psycopg2-binary; python_version < "3.13"
psycopg[binary]; python_version >= "3.13"
python-dotenv
pydantic>=2.0
requests
python-multipart

# Dashboard
jinja2

# AI pipeline
langchain
langchain-community
pymupdf
numpy

# Optional emotion stack:
# Installed separately via requirements-emotion.txt (optional).

# Vector store (FAISS wheels may not be available on native Windows pip; use WSL/Conda if needed)
faiss-cpu; platform_system != "Windows"

# Auth
bcrypt
python-jose[cryptography]

# Migrations
alembic

# Legacy extractors kept for compatibility (can be removed once LangChain pipeline is default)
PyPDF2
python-docx
